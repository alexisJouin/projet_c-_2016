#include "ArrayOfNumber.hpp"

void ArrayOfNumber::generateNumber(){
  for(int i=0;i<10;i++)
    Number n; //Créer un constructeur Number  qui génère un nombre aléatoire entre 1 et 30.
  _numbers= push.back(n);
}
